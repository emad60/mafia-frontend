/**
 * Custom hooks — ready for backend integration.
 *
 * Planned:
 *  - useGameSocket(url)  → WebSocket connection to game server
 *  - usePlayerList()     → synced player roster
 *  - useGameEvents()     → typed event stream (phase changes, votes, kills)
 */

export {}
